export interface T { // it allows our controller to have many functions with different names and properties.
    [key: string]: any; // key -> string, value ->
} 
// interface is a rule, blueprint for objects


